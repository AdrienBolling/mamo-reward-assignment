"""Environments written for this project.

These environments are small and controllable. They give ground truth about the
conflict between channels, and they make a failure cheap to find. An outside
environment does not belong here. It belongs in ``mamora.connectors``.

This layer imports ``mamora.contract`` and nothing else from ``mamora``.
"""
